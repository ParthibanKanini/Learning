import { test } from '../fixtures/login.fixtures.js';

test.describe('Seed for login user', () => {
  test('seed using login fixture', async ({ pageFactory }): Promise<void> => {
    const pageCreator = pageFactory;
  });
});
